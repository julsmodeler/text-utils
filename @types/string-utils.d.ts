export declare class StringUtils {
    private constructor();
    static replaceAll(str: string, srch: string, repl: string): string;
    static escapeRegExp(str: string): string;
    static removeTrailingNewlineIfAny(str: string): string;
}
